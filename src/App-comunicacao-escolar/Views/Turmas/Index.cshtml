@model IEnumerable<App_comunicacao_escolar.Models.Turma>

@{
    ViewData["Title"] = "Index";
}

<h1>Lista de turmas</h1>

<div class="d-flex justify-content-between my-4">
    <a class="btn btn-primary" asp-action="Create">Cadastrar turma</a>
</div>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Codigo)
            </th>
            <th>
                
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Codigo)
            </td>
            <td>
                <a class="btn btn-primary p-1" asp-action="AdicionarDisciplinas" asp-route-id="@item.Id">Associar disciplinas</a>
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
